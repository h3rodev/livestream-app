# Livestream App – Phase 1 (WebRTC Prototype)

This repo contains a minimal WebRTC prototype:

- Node + Socket.IO signalling server
- `admin.html` – admin viewer
- `camera.html` – browser-based camera client (open on phone)

## Run

```bash
cd server
npm run dev
